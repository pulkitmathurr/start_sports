<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=0">
    <title>
        <%= title %> - Start Sports
    </title>

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/images/startSportLogo1.png">
    <link rel="shortcut icon" href="/images/AdminLogo1.png">

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- DashQuant CSS — local (PEHLE load karo) -->
    <link href="/assets/css/style.css" rel="stylesheet">
    <link href="/assets/css/plugins.css" rel="stylesheet">

    <!-- Our custom CSS — BAAD MEIN load karo taaki override kare -->
    <link rel="stylesheet" href="/css/style.css">
    <!-- Google Fonts — same as login -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Rajdhani:wght@400;500;600;700&family=Inter:wght@300;400;500;600&display=swap"
        rel="stylesheet">
    <!-- Icons CDN -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/boxicons@2.1.4/css/boxicons.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css">

    <script>
        // ── bfcache guard ──────────────────────────────────────────────
        (function () {
            window.addEventListener('pageshow', function (e) {
                if (e.persisted) { window.location.reload(); }
            });
            sessionStorage.setItem('ss_auth_loaded', Date.now());
        })();

        // ── CSRF helper ────────────────────────────────────────────────
        // Defined HERE (in header) so it is available to every inline
        // <script> block on the page — including quickbook-drawer.ejs
        // which is rendered before footer.ejs.
        function getCsrfToken() {
            const match = document.cookie.match(/(?:^|; )csrf_token=([^;]*)/);
            return match ? decodeURIComponent(match[1]) : '';
        }

        // ── Patched fetch ──────────────────────────────────────────────
        // Automatically injects X-CSRF-Token on all mutating requests.
        // Must live in header so the patch is in place before ANY inline
        // script (quickbook-drawer, page scripts) calls fetch().
        // footer.ejs previously defined this, but it ran too late —
        // after quickbook-drawer.ejs scripts had already been parsed.
        (function () {
            const _originalFetch = window.fetch;
            window.fetch = function (url, options) {
                options = options || {};
                const method = (options.method || 'GET').toUpperCase();
                if (['POST', 'PUT', 'PATCH', 'DELETE'].includes(method)) {
                    options.headers = options.headers || {};
                    if (options.headers instanceof Headers) {
                        options.headers.set('X-CSRF-Token', getCsrfToken());
                    } else {
                        options.headers['X-CSRF-Token'] = getCsrfToken();
                    }
                }
                return _originalFetch.call(this, url, options);
            };
        })();
    </script>

</head>

<body class="app sidebar-mini ltr light-mode">

    <!-- Loader -->
    <!-- <div id="global-loader">
        <img src="/assets/images/loader.svg" class="loader-img" alt="Loader">
    </div> -->

    <div class="page">
        <div class="page-main">

            <!-- APP HEADER -->
            <div class="app-header header sticky">
                <div class="container-fluid main-container">
                    <div class="d-flex">

                        <a aria-label="Hide Sidebar" class="app-sidebar__toggle" data-bs-toggle="sidebar"
                            href="javascript:void(0)">
                            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24"
                                fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
                                stroke-linejoin="round"
                                class="feather feather-grid status_toggle middle sidebar-toggle">
                                <rect x="3" y="3" width="7" height="7"></rect>
                                <rect x="14" y="3" width="7" height="7"></rect>
                                <rect x="14" y="14" width="7" height="7"></rect>
                                <rect x="3" y="14" width="7" height="7"></rect>
                            </svg>
                        </a>

                        <!-- Right side: dark mode + profile — always visible -->
                        <div class="d-flex align-items-center gap-2 ms-auto header-right-icons">

                            <!-- Profile -->
                            <div class="dropdown d-flex profile-1">
                                <a href="javascript:void(0)" data-bs-toggle="dropdown" data-bs-offset="0,2"
                                    class="nav-link user-dropdown">
                                    <div class="avatar avatar-md brround cover-image"
                                        style="overflow:hidden; display:flex; align-items:center; justify-content:center;">
                                        <% if (typeof user !=='undefined' && user && user.profile_image) { %>
                                            <img src="/uploads/avatars/<%= user.profile_image %>" alt="<%= user.name %>"
                                                style="width:100%; height:100%; object-fit:cover; border-radius:50%;">
                                            <% } else { %>
                                                <%= typeof user !=='undefined' && user && user.name ?
                                                    user.name.charAt(0).toUpperCase() : 'A' %>
                                                    <% } %>
                                    </div>
                                    <div class="media-body d-lg-block d-none ps-2">
                                        <p class="mb-0">
                                            <%= typeof user !=='undefined' && user && user.name ? user.name : 'Admin' %>
                                        </p>
                                        <p class="mb-0 text-muted" style="font-size:0.75rem;">Administrator</p>
                                    </div>
                                </a>
                                <div class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                                    <div class="drop-heading">
                                        <div class="d-flex align-items-center gap-3 px-1">
                                            <!-- Avatar -->
                                            <div style="width:46px; height:46px; border-radius:50%; overflow:hidden; flex-shrink:0;
                    background:linear-gradient(135deg,#1B4332,#39ff14);
                    display:flex; align-items:center; justify-content:center;
                    font-weight:700; font-size:1.1rem; color:#fff;">
                                                <% if (typeof user !=='undefined' && user && user.profile_image) { %>
                                                    <img src="/uploads/avatars/<%= user.profile_image %>"
                                                        alt="<%= user.name %>"
                                                        style="width:100%; height:100%; object-fit:cover;">
                                                    <% } else { %>
                                                        <%= typeof user !=='undefined' && user && user.name ?
                                                            user.name.charAt(0).toUpperCase() : 'A' %>
                                                            <% } %>
                                            </div>
                                            <!-- Name + Role -->
                                            <div>
                                                <h5 class="text-dark mb-0 fs-14 fw-semibold">
                                                    <%= typeof user !=='undefined' && user && user.name ? user.name
                                                        : 'Admin' %>
                                                </h5>
                                                <small class="text-muted">Administrator</small>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="dropdown-divider m-0"></div>
                                    <a class="dropdown-item" href="/profile">
                                        <i class="dropdown-icon bi bi-person"></i> Profile
                                    </a>
                                    <div class="dropdown-divider m-0"></div>
                                    <form action="/auth/logout" method="POST" style="margin:0;padding:0">
                                        <input type="hidden" name="_csrf"
                                            value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
                                        <button type="submit" class="dropdown-item text-danger"
                                            style="width:100%;text-align:left;background:transparent;border:none;color:#dc3545 !important;">
                                            <i class="dropdown-icon bi bi-box-arrow-right" style="color:#dc3545"></i>
                                            Logout
                                        </button>
                                    </form>
                                </div>
                            </div>

                        </div>
                    </div>
                </div>
            </div>
            <!-- APP HEADER END -->

            <!-- APP SIDEBAR -->
            <div class="sticky">
                <div class="app-sidebar__overlay" data-bs-toggle="sidebar"></div>
                <div class="app-sidebar">
                    <div class="side-header">
                        <a class="header-brand1" href="/dashboard">
                            <!-- Full logo — visible when expanded -->
                            <img src="/images/AdminLogo1.png" alt="SS Logo" class="sidebar-logo sidebar-logo--full"
                                style="height:180px; width:auto; object-fit:contain;">
                            <!-- Mini logo — visible when collapsed -->
                            <img src="/images/logoCollapsed1.png" alt="SS" class="sidebar-logo sidebar-logo--mini"
                                style="display:none; height:150px; width:100px; object-fit:contain; border-radius:8px;">
                        </a>
                    </div>

                    <div class="main-sidemenu">
                        <ul class="side-menu">

                            <li class="sub-category">
                                <h3>Main</h3>
                            </li>

                            <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'dashboard' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/dashboard">
                                    <i class="side-menu__icon bi bi-house"></i>
                                    <span class="side-menu__label">Dashboard</span>
                                </a>
                            </li>

                            <li class="sub-category">
                                <h3>Bookings</h3>
                            </li>

                            <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'bookings' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/bookings">
                                    <i class="side-menu__icon bi bi-calendar-check"></i>
                                    <span class="side-menu__label">Bookings</span>
                                </a>
                            </li>

                            <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'history' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/history">
                                    <i class="side-menu__icon bi bi-clock-history"></i>
                                    <span class="side-menu__label">History</span>
                                </a>
                            </li>

                            <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'customers' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/customers">
                                    <i class="side-menu__icon bi bi-people"></i>
                                    <span class="side-menu__label">Customers</span>
                                </a>
                            </li>

                            <li class="sub-category">
                                <h3>Financial</h3>
                            </li>

                            <!-- Balance Sheet Menu Item -->
                            <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'balance' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/balance-sheet/page">
                                    <i class="side-menu__icon bi bi-calculator-fill"></i>
                                    <span class="side-menu__label">Balance Sheet</span>
                                </a>
                            </li>

                            <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'expenses' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/expenses/page">
                                    <i class="side-menu__icon bi bi-wallet2"></i>
                                    <span class="side-menu__label">Expense Tracker</span>
                                </a>
                            </li>

                            <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'reports' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/reports">
                                    <i class="side-menu__icon bi bi-bar-chart-line"></i>
                                    <span class="side-menu__label">Reports</span>
                                </a>
                            </li>
                            <li class="sub-category">
                                <h3>Admin</h3>
                            </li>

                            <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'grounds' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/grounds">
                                    <i class="side-menu__icon bi bi-geo-alt"></i>
                                    <span class="side-menu__label">Manage Grounds</span>
                                </a>
                            </li>

                            <!-- <li class="sub-category">
                                <h3>Academy</h3>
                            </li> -->

                            <!-- <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'academy-coaches' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/academy/coaches">
                                    <i class="side-menu__icon bi bi-person-badge"></i>
                                    <span class="side-menu__label">Coaches</span>
                                </a>
                            </li>
                            <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'academy-students' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/academy/students">
                                    <i class="side-menu__icon bi bi-mortarboard"></i>
                                    <span class="side-menu__label">Students</span>
                                </a>
                            </li> -->
                            <!-- <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'academy-batches' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/academy/batches">
                                    <i class="side-menu__icon bi bi-collection"></i>
                                    <span class="side-menu__label">Batches</span>
                                </a>
                            </li>
                            <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'academy-attendance' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/academy/attendance/mark">
                                    <i class="side-menu__icon bi bi-pencil-square"></i>
                                    <span class="side-menu__label">Attendance</span>
                                </a>
                            </li> -->

                            <!-- <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'academy-fees' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/academy/fees">
                                    <i class="side-menu__icon bi bi-cash-coin"></i>
                                    <span class="side-menu__label">Fees</span>
                                </a>
                            </li> -->
                            <li class="sub-category">
                                <h3>Account</h3>
                            </li>

                            <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'profile' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/profile">
                                    <i class="side-menu__icon bi bi-person"></i>
                                    <span class="side-menu__label">Profile</span>
                                </a>
                            </li>

                            <li
                                class="slide <%= typeof activePage !== 'undefined' && activePage === 'billing' ? 'is-expanded' : '' %>">
                                <a class="sidenav-menu-item" href="/billing" style="display:flex;align-items:center;">
                                    <i class="side-menu__icon bi bi-credit-card"></i>
                                    <span class="side-menu__label" style="flex:1">Billing & Subscription</span>
                                    <% const _sub=(typeof subStatus !=='undefined' ) ? subStatus : null; %>
                                        <% if (_sub && _sub.isExpired) { %>
                                            <span class="billing-sb-badge billing-sb-badge--expired">Expired</span>
                                            <% } else if (_sub && _sub.isExpiringSoon) { %>
                                                <span class="billing-sb-badge billing-sb-badge--warn">
                                                    <%= _sub.daysLeft %>d
                                                </span>
                                                <% } else if (_sub && _sub.isTrial) { %>
                                                    <span class="billing-sb-badge billing-sb-badge--trial">Trial</span>
                                                    <% } %>
                                </a>
                            </li>

                            <li class="slide">
                                <form action="/auth/logout" method="POST" style="margin:0;padding:0">
                                    <input type="hidden" name="_csrf"
                                        value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
                                    <button type="submit" class="sidenav-menu-item"
                                        style="background:transparent;border:none;width:100%;text-align:left;padding:0;color:#ef4444;">
                                        <i class="side-menu__icon bi bi-box-arrow-right" style="color:#ef4444"></i>
                                        <span class="side-menu__label" style="color:#ef4444">Logout</span>
                                    </button>
                                </form>
                            </li>

                            <% const _sc=(typeof supportContact !=='undefined' ) ? supportContact : {}; const
                                _hasPhone=_sc.support_phone && _sc.support_phone.trim() !=='' ; const
                                _hasEmail=_sc.support_email && _sc.support_email.trim() !=='' ; %>
                                <% if (_hasPhone || _hasEmail) { %>
                                    <li class="slide need-help-block" style="padding: 10px 12px 6px;">
                                        <div style="background:rgba(27,67,50,0.07); border:1px solid rgba(27,67,50,0.15);
                                            border-radius:10px; padding:11px 12px;">
                                            <div
                                                style="font-size:0.7rem; font-weight:700; color:rgba(255,255,255,0.85); letter-spacing:0.5px; text-transform:uppercase; margin-bottom:4px;">
                                                <i class="bi bi-headset me-1"></i> Need Help?
                                            </div>
                                            <% if (_sc.support_hours && _sc.support_hours.trim() !=='' ) { %>
                                                <div style="font-size:0.66rem; color:#6B7280; margin-bottom:8px;">
                                                    <i class="bi bi-clock me-1"></i>
                                                    <%= _sc.support_hours %>
                                                </div>
                                                <% } %>
                                                    <div style="display:flex; flex-direction:row; gap:6px;">
                                                        <% if (_hasPhone) { %>
                                                            <a href="https://wa.me/91<%= _sc.support_phone.replace(/\D/g,'') %>?text=Hi%2C%20I%20need%20support%20with%20my%20admin%20panel"
                                                                target="_blank" style="flex:1; display:inline-flex; align-items:center; justify-content:center;
       padding:5px 0; border-radius:7px; font-size:0.95rem;
       background:rgba(37,211,102,0.15); color:#4ade80;
       border:1px solid rgba(37,211,102,0.3); text-decoration:none;">
                                                                <i class="bi bi-whatsapp"></i>
                                                            </a>
                                                            <% } %>
                                                                <% if (_hasEmail) { %>
                                                                    <a href="https://mail.google.com/mail/?view=cm&to=<%= encodeURIComponent(_sc.support_email) %>&su=<%= encodeURIComponent('Support Request - ' + (typeof tenant !== 'undefined' && tenant ? tenant.business_name : 'Admin Panel')) %>"
                                                                        target="_blank" style="flex:1; display:inline-flex; align-items:center; justify-content:center;
       padding:5px 0; border-radius:7px; font-size:0.95rem;
       background:rgba(255,255,255,0.1); color:rgba(255,255,255,0.85);
       border:1px solid rgba(255,255,255,0.2); text-decoration:none;">
                                                                        <i class="bi bi-envelope"></i>
                                                                    </a>
                                                                    <% } %>
                                                    </div>
                                        </div>
                                    </li>
                                    <% } %>

                        </ul>
                    </div>
                </div>
            </div>
            <!-- APP SIDEBAR END -->

            <!-- PAGE WRAPPER START -->
            <div class="app-content main-content">
                <div class="side-app">
                    <div class="main-container container-fluid">